{
  "name": "therapi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runtime API discovery tool (Capture -> Infer -> Expose)",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js"
  }
}
